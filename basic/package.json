{
  "name": "basic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "start:watch": "nodemon --watch './src' --watch 'tsconfig.json' --ext 'js,ts,jsx,tsx' --exec 'npm run build && node dist/app.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "@types/node": "^18.15.11",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@apollo/server": "^4.6.0",
    "graphql": "^16.6.0",
    "ts-node": "^10.9.1",
    "uuid": "^9.0.0"
  }
}
